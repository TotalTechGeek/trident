$out: {{content.name}}/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{content.name}}
  labels:
    team: {{content.team}}
spec:
  replicas: {{content.replicas}}
  selector:
    matchLabels:
      app: {{content.name}}
